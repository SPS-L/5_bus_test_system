 
# Normalize all text files to LF in the repository
* text=auto

# Force LF for specific file types
*.sh eol=lf
*.py eol=lf
*.f90 eol=lf
Makefile

# Force CRLF for specific file types (e.g., Windows-specific files)
*.bat eol=crlf
*.sln eol=crlf
*.vfproj eol=crlf
